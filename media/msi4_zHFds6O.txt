clc; 
clear all; 
close all;

% wczytanie danych
Dane = load('Data_PTC_vs_FTC.mat');
X = Dane.Data.X; % dane dla 86 pacjentow z wartosciami ekspresji 1000 genów 
D = Dane.Data.D; % dane oznaczajace przynaleznosc do klasy 

% przygotowanie danych do dalszej analizy
[x, y] = size(X);
X(x+1,:) = D;
classificationLearner;
pause;